# Ubuntu Voice to Text Configuration
# Copy this file to .env and configure your settings

# Voice Recognition Source Selection
# Options: vosk (offline, default) or whisper (OpenAI API)
RECOGNITION_SOURCE=vosk

# OpenAI Whisper Configuration (only needed if RECOGNITION_SOURCE=whisper)
# Get your API key from: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here
WHISPER_MODEL=whisper-1

# Vosk Configuration (only needed if RECOGNITION_SOURCE=vosk)
# Path to Vosk model (for manual installation, Docker manages this automatically)
# VOSK_MODEL_PATH=/path/to/vosk-model-small-en-us-0.15

# Docker Configuration (automatically set by Docker Compose)
# DISPLAY=${DISPLAY}
# PULSE_RUNTIME_PATH=/run/user/1000/pulse
# XDG_RUNTIME_DIR=/run/user/1000  
# XAUTHORITY=/home/${USER}/.Xauthority